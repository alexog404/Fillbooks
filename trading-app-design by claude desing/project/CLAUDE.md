# Chart style preference
For every line/area chart (sparklines, equity curves, etc.), use a filled area under the line that fades from the line's color (semi-opaque) at top down to transparent at the bottom — not a plain stroke-only line. Apply this consistently to all future graphs in this project.
